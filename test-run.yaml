name: "basic-test"

dataset:
  name: "verified"
  split: ":5"

execution:
  runs: 1
  max_parallel: 4
  timeout_sec: 600

model:
  name: "claude-opus-4-5"

configs:
  - name: "baseline"
    description: "All tools enabled, no plugins"

output_dir: "./results/basic-test"
